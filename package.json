{
  "name": "developers",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "git config core.hooksPath .githooks",
    "check:rules": "node scripts/check-rules.mjs"
  },
  "devDependencies": {
    "rumdl": "0.2.27"
  }
}
