---
id: DEV-350
title: "Mark a Design PR docs(ui) With a Design Section"
status: "active"
enforcement: "manual"
severity: "error"
depends_on: ["DEV-340"]
---

## Mark a Design PR docs(ui) With a Design Section

### Problem

Design work without a consistent type and scope, or without a structured design
reference, is hard to find, track, and review against the intended screens.

### Solution

Give design work a consistent home.

1. Use `docs(ui)` as the type and scope, for example
   `docs(ui): design table component`.
1. Add a `## Design` section to the relevant Spec file, structured with this
   markup:

```text
## Design
- [/page](https://figma.com/your-design-file-url)
  - ./page/{params}
    - (group)
      - [[state]](https://figma.com/your-design-file-url)
```

Key: `/...` a page, `{...}` a dynamic URL parameter, `(...)` a grouping of
related features or components, `[...]` a specific state (for example a popup),
and indentation represents nesting.

#### Acceptance Criteria

- [ ] The design PR uses the `docs(ui)` type and scope
- [ ] A `## Design` section is added to the relevant Spec
- [ ] The section uses the page, parameter, group, and state markup
